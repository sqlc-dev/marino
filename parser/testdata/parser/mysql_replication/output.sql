PURGE BINARY LOGS TO 'binlog.000001'
-- case
PURGE BINARY LOGS BEFORE _UTF8MB4'2026-08-18 00:00:00'
-- case
PURGE BINARY LOGS BEFORE DATE_SUB(NOW(), INTERVAL 3 DAY)
-- case
RESET BINARY LOGS AND GTIDS
-- case
CHANGE REPLICATION FILTER REPLICATE_DO_DB = (`db1`)
-- case
CHANGE REPLICATION FILTER REPLICATE_DO_DB = (`db1`, `db2`), REPLICATE_IGNORE_DB = (`db3`)
-- case
CHANGE REPLICATION FILTER REPLICATE_DO_TABLE = (`db1`.`t1`, `db2`.`t2`)
-- case
CHANGE REPLICATION FILTER REPLICATE_IGNORE_TABLE = (`db1`.`t1`)
-- case
CHANGE REPLICATION FILTER REPLICATE_WILD_DO_TABLE = ('db1.old%')
-- case
CHANGE REPLICATION FILTER REPLICATE_WILD_IGNORE_TABLE = ('db1.new%', 'db2.%')
-- case
CHANGE REPLICATION FILTER REPLICATE_REWRITE_DB = ((`db1`, `db2`))
-- case
CHANGE REPLICATION FILTER REPLICATE_REWRITE_DB = ((`db1`, `db2`), (`db3`, `db4`)) FOR CHANNEL 'ch1'
-- case
CHANGE REPLICATION FILTER REPLICATE_DO_DB = ()
-- case
RESET REPLICA
-- case
RESET REPLICA ALL
-- case
RESET REPLICA ALL FOR CHANNEL 'ch1'
-- case
START REPLICA
-- case
START REPLICA IO_THREAD
-- case
START REPLICA IO_THREAD, SQL_THREAD
-- case
START REPLICA UNTIL SQL_BEFORE_GTIDS = '3E11FA47-71CA-11E1-9E33-C80AA9429562:11-56'
-- case
START REPLICA UNTIL SQL_AFTER_GTIDS = '3E11FA47-71CA-11E1-9E33-C80AA9429562:11-56'
-- case
START REPLICA UNTIL SOURCE_LOG_FILE = 'source1-bin.000291', SOURCE_LOG_POS = 137
-- case
START REPLICA SQL_THREAD UNTIL RELAY_LOG_FILE = 'replica-relay-bin.000015', RELAY_LOG_POS = 5722
-- case
START REPLICA UNTIL SQL_AFTER_MTS_GAPS
-- case
START REPLICA USER = 'u' PASSWORD = 'p' DEFAULT_AUTH = 'auth_plugin' FOR CHANNEL 'ch1'
-- case
STOP REPLICA
-- case
STOP REPLICA SQL_THREAD FOR CHANNEL 'ch1'
-- case
STOP REPLICA IO_THREAD, SQL_THREAD
-- case
START GROUP_REPLICATION
-- case
START GROUP_REPLICATION USER = 'u', PASSWORD = 'p', DEFAULT_AUTH = 'auth_plugin'
-- case
STOP GROUP_REPLICATION
-- case
SELECT `filter`,`gtids`,`io_thread`,`sql_thread` FROM `group_replication`
